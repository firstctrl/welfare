{
  "name": "@welfare/web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet' && exit 0"
  }
}
